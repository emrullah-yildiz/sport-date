# Agent state

## Latest work cycle - 2026-07-04 (join-request decision notifications)

**Requesters are proactively informed when their request reaches a real outcome.** A successful host approval now sends one transactional email linking the accepted member to the authorized event room; a final third-skip decline sends a private, dignity-preserving closed-request notice. Intermediate skips send nothing and neither email reveals skip history, host reasoning, or the exact meeting address. Browser and mobile decisions share the same post-commit notification boundary, delivery remains fail-closed behind the approved Gmail configuration, and email failure cannot undo the authoritative in-app decision. Verification: full suite green (940 web tests passed, 12 skipped; 218 domain tests passed), all workspace typechecks green, production build green, and lint green with two pre-existing warnings.

## Latest work cycle - 2026-07-04 (Gmail transactional delivery)

**Owner-approved Gmail delivery is code-complete and dark until credentials land.** A single server-only Gmail API sender now covers verification, password reset, T-2h attendance reminders, and feedback updates. It uses short-lived access tokens obtained from the owner-authorized refresh token, sends multipart text/HTML, logs neither credentials nor message bodies, and fails closed unless the provider flag, OAuth client/secret/refresh token, verified sender alias, and canonical app origin are all configured. `.env.production.example` and the HQ blocker card now state the exact remaining owner steps. Verification: full web suite green (932 passed, 12 skipped), web typecheck and production build green, lint green with two pre-existing warnings. Remaining owner action: verify `support@keepitup.social` as Gmail “Send mail as,” create/authorize the narrow Gmail OAuth client, store secrets directly in Vercel, then authorize a single live test send before enabling delivery.

## Latest work cycle - 2026-07-04 (host request controls)

**Pending join requests are actionable again.** The host queue had drifted from the relaxed join rule: members may request without listing the event sport, but an inner `user_sports` join silently removed their request card and its controls. The queue and accepted-participant room query now retain those members with a “not listed” skill fallback. Hosting cards with pending requests deep-link directly to `#join-requests`, the primary decision now reads “Approve request,” and members with a pending/accepted request see “Manage request” from discovery so cancellation is findable. Host authorization and post-acceptance exact-location gating are unchanged. Regression coverage pins the optional-sport case and host-owned query boundary. Verification: full web suite green (923 passed, 12 skipped), web typecheck and production build green, lint green with two pre-existing warnings.

## Latest work cycle - 2026-07-04

**Exact event pin selection is implemented.** Event create/edit now offers an accessible debounced address search, requires the host to choose a suggestion, persists its precise coordinates in the existing private-location boundary, and makes “Get directions” target that pin. An authenticated, rate-limited, no-store server proxy data-minimizes Photon/OpenStreetMap results and avoids sending member identity, cookies, or client IP to the geocoder. Exact pins remain limited to hosts and accepted participants. Provider availability is the remaining operational risk; `LOCATION_SEARCH_BASE_URL` supports a reviewed/self-hosted replacement before scale. Verification: live Photon response checked; full web suite green (922 passed, 12 skipped), lint green with two pre-existing warnings, web typecheck green, and the Next.js production build green.

## Planning snapshot — 2026-07-01 (Planner)

**Current focus:** Product is fully functional and prod is healthy; all P0/P1 are done and verified. The loop is now grooming and closing the remaining trust/safety, recovery, and vibe polish so the free core loop is credible before any monetization work begins.

**Single next ticket to build:** `CX-20260630-event-detail-safety-emergency-microcopy-smallest-text` (reprioritized P2 → **P1** this pass). The emergency-services safety line is the smallest text on the page (11px vs 17px body); the safety-and-accessibility-never-below-P1 guardrail floors it to P1. Lowest-effort P1 (CSS on a few `.safety-*` rules) and the shared `ReportSafetyControls` fix lands on event detail, event room, and host-request cards at once.

**Next 3 outcomes (after the P1 above):**
1. `CX-20260701-safety-center-report-tracker-only-no-proactive-guidance-link` (P2, RICE 32) — connect the Safety center to the existing `/safety-guidelines` and reframe its dead-end empty state; highest-scoring ready trust ticket, no owner decision.
2. `CX-20260701-reset-verify-dead-link-no-direct-recovery-path` (P2, RICE 24) — one-click fresh-link recovery on dead reset/verify pages, anti-enumeration preserved; closes the last big re-entry dead-end (feature-roadmap a3).
3. `CX-20260701-event-room-stays-future-tense-after-event-ends` (P2) — event-room lifecycle correctness (future-tense after end → graceful transition to reflection); completes the activity→reflection journey (feature-roadmap a6).

**Owner blockers (stay `blocked-owner`, do NOT let Builder pull):**
- `CX-20260701-owner-decision-payments-processor-and-billing-gate` (P1) — no billing until a processor is chosen.
- `CX-20260701-owner-decision-brand-name-and-logo` (P1) — brand/name/logo; blocks `brand-asset-swap-mechanism` and the research serif choice.
- `CX-20260701-owner-action-run-surveys-and-forum-outreach` (P2) — external outreach needs the owner.
- `CX-20260701-no-automatic-production-migration-on-deploy` (P0) — standing production-ops rule; owner-governed, no auto-migrate on deploy.
- (Recommendation on all four: hold; each has a clear recommendation in-ticket. Monetization c-track waits behind payments + pricing.)

**Open risks:**
- A cluster of `implemented`-but-not-yet-`verified` tickets awaits the Tester: `empty-states-lack-warmth` (P0), `first-event-preparation-card` (P1), `global-error-boundary-white-screen` (P1), `home-landing-not-auth-aware` (P1), `peer-feedback-submit-no-focus-confirmation` (P1), `remember-me-optional-persistent-login` (P2). None are known-broken, but they are unproven live — verification is the gating activity, not new build.
- Signup/login rate limits (5/hr/IP signup, 10/15min browser-auth) repeatedly block live retest of two-account flows; verification of multi-account tickets can lag. Do not poll for limit resets.
- Monetization/photos value is real but correctly gated behind owner decisions (payments, storage/moderation); backlog reflects this — no un-ticketed high-value work outranks the current queue (feature-roadmap-proposal confirms its top items are all already filed).

## Current outcome

**Owner finalized the legal set — drafts promoted to owner-approved operational documents in `docs/legal/` (honest non-counsel header).** On the owner's "legal looks OK, finalize it" instruction, one `docs:` commit promoted the four `docs/legal/drafts/` documents to finalized, canonical documents: `docs/legal/privacy-notice.md`, `docs/legal/terms-of-service.md`, `docs/legal/safety-community-guidelines.md`, `docs/legal/consent-copy.md`, plus a new `docs/legal/README.md` index. Each "DRAFT — requires qualified EU counsel review" header was replaced with a **"PUBLISHED — owner-approved as of 2026-06-30"** header that is honest: operational, owner-approved, **not legal advice, not lawyer-reviewed**, with independent qualified-EU-counsel review still recommended before relying on them in a dispute — preserving the AGENTS.md rule against representing legal advice as counsel. The `[OWNER:]`/`[COUNSEL:]` markers were resolved onto the working assumptions (name "Sport Date", EU/GDPR baseline, Bucharest *hypothesis*, implemented data-minimisation defaults); retention uses the **actually-implemented** periods where the code fixes one (browser session 7d, mobile refresh 30d, verification 24h, reset 60m, appeal window 6 months) and states an owner/counsel-confirmable operating default otherwise. Owner-only facts (legal entity name & registered address, a monitored privacy/safety contact email, DPO/representative if any, processor list + DPAs, named safety owner, real links + enabled email provider, backup schedule, final jurisdiction) were **not invented** — they are `[OWNER TO CONFIRM: …]` placeholders, consolidated into an "Owner inputs still required to be litigation-ready" list per document and in `docs/legal/README.md`. Nothing was overclaimed beyond the implemented schema/flows; all honesty boundaries intact. The drafts README now marks the drafts SUPERSEDED and points up; `privacy-rights-preparation.md`, `decision-log.md`, and `owner-escalation.md` (Gate 2) updated to record the owner finalization while noting qualified-counsel review + the owner-fact placeholders remain open. **Verify:** docs-only; typecheck green; hermetic web suite unchanged at 129 passed + 12 skipped. (Pre-existing, unrelated: the date-sensitive `packages/domain/src/registration.test.ts` line 23 now fails because 2008-06-30 turns 18 today, 2026-06-30 — it omits an explicit "now" date and uses the system clock; not touched by this docs work.)

## Previous outcome

**Sentry error monitoring wired into the Next.js web app — env-gated, PII-scrubbed, build-plugin verified on Next 16.2.9.** One `feat:` commit; **first new dependency added** (`@sentry/nextjs@^10.62.0`, whose peer range includes `^16.0.0-0`, so Next 16.2.9 is officially supported). `withSentryConfig` **builds cleanly on Next 16.2.9 (Turbopack) — no fallback was needed.** Everything is gated on `process.env.NEXT_PUBLIC_SENTRY_DSN`: with NO DSN set (the current production state), every `Sentry.init` is a no-op and runtime behaviour is **identical to today** (proven by the unchanged 122 web tests + green no-DSN/no-token build). Added: `apps/web/sentry.server.config.ts`, `apps/web/sentry.edge.config.ts`, `apps/web/src/instrumentation-client.ts` (client init, `onRouterTransitionStart` export), `apps/web/src/instrumentation.ts` (`register()` importing the server/edge config by `NEXT_RUNTIME`; `onRequestError = Sentry.captureRequestError`), `apps/web/src/app/global-error.tsx` (App Router error boundary reporting to Sentry), and a shared `apps/web/src/lib/sentry-scrub.ts` PII scrubber wired as `beforeSend` on all three runtimes. **Privacy (EU dating product):** `sendDefaultPii: false`; **Session Replay OFF** (no Replay integration; `replaysSessionSampleRate`/`replaysOnErrorSampleRate` both 0); `tracesSampleRate: 0.1`; `environment: VERCEL_ENV || NODE_ENV`. `beforeSend` strips request bodies/`data`, `query_string`, `cookies`, `env`; drops `Cookie`/`Authorization`/`X-Forwarded-For`/`X-Real-IP` headers (case-insensitive); removes the URL query string (keeps path); deletes the top-level `user` object and `server_name`; and deep-scrubs every email and IPv4/IPv6 from message/extra/tags/contexts/breadcrumbs/exception (bounded depth; drops the whole event on any scrub error). `next.config.ts` is wrapped with `withSentryConfig` **preserving `headers()` and `transpilePackages`**: `silent: true`, `telemetry: false`, `widenClientFileUpload: true`, `org`/`project` from env, `authToken` from `SENTRY_AUTH_TOKEN`, and **`sourcemaps.disable = !SENTRY_AUTH_TOKEN`** so with no token the build does NO source-map upload and emits no error. New `apps/web/src/lib/sentry-scrub.test.ts` (7 tests) pins the scrubbing contract (user/server_name dropped; body/query/cookies/env stripped; URL path kept but query removed; sensitive headers removed; email + IPv4 + IPv6 redacted from text; PII-free event untouched; in-place). **Verify:** web hermetic 122 → 129 (+7); full hermetic 179 → 186 (129 web + 57 domain); opt-in DB unchanged at 12; typecheck green; **production build green with no DSN and no auth token (the current production scenario)**. No `/sentry-example` debug route committed. **Owner remaining:** paste `NEXT_PUBLIC_SENTRY_DSN` (EU region, owner step 5) into Vercel to activate capture; optionally add `SENTRY_AUTH_TOKEN`+`SENTRY_ORG`+`SENTRY_PROJECT` for source-map upload; then redeploy and trigger a live test error to confirm events land. Sentry is now a **new processor for the Gate-2 (counsel) processor list**.

## Previous outcome

**Operational launch-gate prep for the in-progress first Vercel deploy** — two coherent commits, no new dependency, nothing deployed. **Commit A (`feat:`):** added a **readiness** probe at `apps/web/src/app/api/health/ready/route.ts`, distinct from the existing **liveness** `/api/health`. It performs ONE cheap DB round-trip (`SELECT 1`) via `getDatabase()` and returns `200 { status: "ready" }` on success, `503 { status: "not_ready", reason }` on `DatabaseNotConfiguredError` (`reason: "database_not_configured"`) or any other client/query error (`reason: "database_unreachable"`). It **never leaks** the connection string, query text, or driver message — only a coarse, safe `reason` — so a monitor can tell "process up but DB unreachable" (503 here, 200 on liveness) from a full outage. Same conventions as the health/cron routes: `runtime="nodejs"`, `dynamic="force-dynamic"`, `Cache-Control: no-store`. Test mirrors the cron route test (`server-only` + `@/lib/db` mocked): 200 when the probe resolves, 503 on `DatabaseNotConfiguredError`, 503 when the query rejects, no-store header, and an explicit assertion that **no response body contains the connection string** on either failure path. **Commit B (`docs:`):** wrote `docs/operations/deployment-runbook.md` — concrete to THIS repo and the ratified Neon/Vercel/Upstash/Sentry stack: first-deploy (GitHub import, Root Directory `apps/web`, region `fra1`, the env-var table), migrations (`npm run db:migrate -w @sport-date/web` with `DATABASE_URL`; additive/idempotent; the migration-019 single-line function-body / `;`+newline split caveat), forward-only rollback (Vercel instant rollback; **no down-migrations** by design + how to handle a bad/destructive migration), secret rotation (provider → Vercel env → redeploy, per secret), backup/recovery (Neon PITR restore-to-branch), the health-vs-readiness table + uptime target, the Vercel-Cron `CRON_SECRET` session-cleanup job, and an incident-response checklist. Honest `[OWNER:]`/`[COUNSEL:]` markers for the still-undecided named on-call owner, RTO/RPO targets, rotation cadence, and alert routing — no invented SLAs/owners/capabilities. Pointers added from `owner-escalation.md` (Gate 1) and this file. **Verify:** web suite hermetic 117 → 122 (+5, the readiness test); full hermetic suite 174 → 179; opt-in DB tests unchanged at 12; typecheck green (web + mobile + domain); production build still compiles with `/api/health/ready` as a dynamic route. Nothing deployed, no account created, no secret added. Note: the Next.js `node_modules/next/dist/docs/` dir is absent in this install (anticipated as possible) — the readiness route reuses the EXACT route-segment conventions the prior cycle validated against those docs (Cache Components OFF → `dynamic` valid) and adds no new routing behavior.

## Previous outcome

**Gate 6 (shared/distributed rate limiting) is now IMPLEMENTED** and activates automatically when the Upstash env vars are present. Made the rate-limit store and its public check functions async end-to-end and dropped in an env-gated Upstash Redis REST adapter behind the existing `RateLimitStore` seam, mirroring the disabled-by-default email-delivery adapter — **no new npm dependency** (`fetch` only). Redefined the store interface around `peek(key, nowMs)` + `bump(key, nowMs, windowMs)` (+ `sweepExpired`/`clear`), all Promise-returning, and kept `consumeRateLimit`'s exact two-phase semantics (peek every rule → decide block with max retry-after, incrementing NOTHING when blocked; then bump every rule only when allowed). The in-memory store stays the DEFAULT when `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are unset and is **behavior-identical** — same fixed-window counting, same `429`/`Retry-After`/`X-RateLimit-Policy` shape, same lazy sweep every 250 mutations, same `resetRateLimitStoreForTests` (now async). `consumeRateLimit`/`enforceRateLimit` now return Promises; **all 12 call sites** (already-async route handlers) gained `await`: web+mobile auth login, register, mobile refresh, web+mobile join-request, web+mobile safety reports, email-verification request+confirm, password-reset request+confirm; five route tests + the limiter's own test now await the reset. **Upstash adapter** (`createUpstashRateLimitStore`, activated only when BOTH env vars set via `resolveRateLimitStore`): atomic fixed-window bump as one REST **pipeline** `[["INCR", key], ["PEXPIRE", key, windowMs]]` against `POST {url}/pipeline` (bearer token), `peek` as `[["GET", key], ["PTTL", key]]` reconstructing `{ count, resetAt: nowMs + ttl }`, same scope/hashed-key/window scheme as in-memory so replicas share one counter, 1s timeout per call. **Graceful degradation:** on ANY Upstash error (thrown/timed-out fetch, non-2xx, non-array payload, embedded `{ error }`) the adapter defers that operation to the in-process in-memory fallback and logs ONE warning — it fails to in-memory enforcement, **never to unlimited**, so an outage degrades shared enforcement to the per-process baseline but never disables abuse protection. New `apps/web/src/lib/rate-limit-upstash.test.ts` (11 tests, mocked `fetch`, no real Upstash/DB): env-gating, under-limit allow + expiry set, at/over-limit block with PTTL-derived retry metadata (through the real `consumeRateLimit`), and error/non-2xx/embedded-error fallback still enforcing the limit; plus an explicit no-env in-memory-unchanged check. Hermetic suite **163 → 174** (117 web + 57 domain); opt-in DB tests unchanged at 12; typecheck green (web + mobile + domain). Residual: the Upstash path is proven with a mocked fetch, not a live instance — closing it needs an opt-in integration test against a real Frankfurt Upstash once the owner provisions it (infrastructure-plan owner step 4).

## Earlier outcome

Closed the migration-019 residual "proven by inference" risk by adding direct real-SQL erasure + immutability tests for the two remaining append-only audit tables, `moderation_case_access_log.actor_user_id` and `moderation_evidence_references.created_by_user_id` — extending `apps/web/src/lib/audit-erasure.integration.test.ts` with 2 opt-in tests (same sentinel-row, transient-`no_delete`-disable, full-cleanup pattern as the existing two). Built the minimal valid FK graph per table from sentinel data only: a reported sentinel user -> a sentinel `safety_reports` row (`report_id`, `ON DELETE RESTRICT`) plus a separate user for the SET-NULL ref — for case-access using `access_type='case_view'` to satisfy the `report_id IS NOT NULL` CHECK, and for evidence filling every NOT NULL/CHECK column (`source_type`, `sensitivity`, `label`, `reference_key`, `preservation_purpose`, `retention_review_at = NOW()+90d`). Each test proves all three properties against real SQL: **(a)** the referenced user is hard-deleted while the audit/reference row SURVIVES with only its user-ref column nulled and every other column unchanged (asserted column-by-column); **(b)** an application UPDATE of a non-user-ref column still RAISES and setting the user-ref column to a non-null value still RAISES; **(c)** an application DELETE is still a no-op (surviving `no_delete` rule). **All four append-only audit tables are now proven DIRECTLY; the "by inference" gap is closed.** Cleanup deletes audit/reference rows by their own primary key (the fix nulls their user columns) via a transient `DISABLE RULE … no_delete` / `ENABLE RULE`, in FK-safe order, then verified the dev DB is pristine: all four `no_delete` rules present (transiently dropped rules restored), all audit tables + `safety_reports` at 0 rows, zero sentinel residue (only the pre-existing `int-test+fixture@sport-date.invalid` auth-email fixture remains), `registrations` untouched. Default suite stays hermetic at **163** (106 web + 57 domain; the now-4 audit-erasure + 8 auth-email integration tests self-skip among the 12 skipped); opt-in DB tests **10 -> 12**; typecheck green.

## Prior outcome

Shipped the credential-free Gate 1 deployment scaffolds so production wiring is a drop-in once the owner provisions Neon EU + Vercel `fra1` + Upstash + Sentry — nothing deployed, no account created, no secret added. Read the Next.js 16.2.9 route-handler / route-segment-config docs first (per `apps/web/AGENTS.md`) and confirmed Cache Components is OFF, so `export const dynamic = "force-dynamic"` is valid; pinned it plus `Cache-Control: no-store` on both new routes. **Commit A (`feat:`)**: (1) `apps/web/src/app/api/health/route.ts` — a `GET` liveness probe returning `200 { status: "ok" }` with **no DB/provider call**, safe for an external uptime monitor before the stack is wired; (2) `apps/web/src/app/api/internal/session-cleanup/route.ts` — a protected endpoint for Vercel Cron that runs the EXISTING `cleanupExpiredSessionResidue` (reused from `session-cleanup.mjs`, no behavior change, its tests untouched), authorized via `Authorization: Bearer ${CRON_SECRET}`, returning 401 on missing/wrong token and **failing closed** (also 401, never runs) when `CRON_SECRET` is unset; implemented as `GET` because Vercel Cron triggers a GET; 503 when the DB is unconfigured; (3) `apps/web/vercel.json` — `regions: ["fra1"]`, a `functions` `maxDuration`, and a daily (`0 3 * * *`) cron hitting `/api/internal/session-cleanup`; (4) `apps/web/.env.production.example` — every production var from the infrastructure plan with placeholders only, plus `.gitignore` `!.env*.example` so templates are tracked and real `.env` stays ignored. **Commit B (`refactor:`)**: put the rate-limit storage behind a small `RateLimitStore` seam (`get`/`set`/`increment`/`sweepExpired`/`clear`) with the current in-memory `Map` as the default (`createInMemoryRateLimitStore`) — behavior-preserving, no new dependency, no Redis code; `rate-limit.test.ts` is byte-for-byte unchanged and green (proof of no behavior change), and the seam documents how the Gate 6 Upstash REST adapter (env-gated, `fetch` INCR+PEXPIRE, in-memory fallback, mirroring the email-adapter pattern) drops in. **Gate 6 is now adapter-ready, awaiting the Upstash store.** New route tests mirror the existing style (`server-only` mocked): health 200 + no-store; cron 401 missing token / 401 wrong token / fail-closed 401 unset-secret / success path / 503 unconfigured. Web suite 99 -> 106; full hermetic suite **156 -> 163** (106 web + 57 domain); opt-in DB unchanged at 10; typecheck green.

## Older outcome

Fixed the systemic user-deletion / audit-immutability conflict that blocked GDPR right-to-erasure of any `users` row. The four append-only audit tables (`moderation_audit_log`, `moderation_case_access_log`, `moderation_evidence_references`, `role_audit_log`) each paired an `ON DELETE SET NULL` FK to `users` with an `ON UPDATE … DO INSTEAD NOTHING` rule, so the FK's internal SET-NULL UPDATE was swallowed and Postgres raised "referential integrity query … gave unexpected result" — no user could be hard-deleted. New migration `apps/web/db/019_audit_append_only_allows_user_nulling.sql` drops each `no_update` rule and replaces it with a `BEFORE UPDATE` trigger backed by one shared PL/pgSQL function (`audit_append_only_allow_user_nulling`, parameterised by the user-ref column names via `TG_ARGV`). The trigger compares `OLD`/`NEW` as `jsonb`: any change to a non-user-ref column raises, and a user-ref column may change only from NON-NULL to NULL (the exact shape of the FK SET-NULL action) — setting a user-ref column to a non-null value also raises. The four `no_delete` rules are kept, so audit rows remain app-undeletable; only the system FK SET-NULL is newly permitted. The migration runner splits on `;`+newline, which would shred a multi-line `$$`-quoted body, so the `CREATE FUNCTION` body is authored on a single physical line (no internal `;` ends a line); verified by simulating the split and by applying `019` cleanly to the dev DB. Proven against real SQL with opt-in tests in `apps/web/src/lib/audit-erasure.integration.test.ts` (2 tests, `RUN_DB_INTEGRATION=1`, sentinel rows, full cleanup) for both `moderation_audit_log` and `role_audit_log`: (a) a referenced user is now hard-deletable, the audit row SURVIVES with the user-ref column NULL and every other column unchanged (incl. `created_at` and `metadata`); (b) an application UPDATE of a non-user-ref column is still REJECTED (and a user-ref → non-null UPDATE is rejected); (c) an application DELETE of an audit row is still a no-op (row remains). The other two tables share the identical trigger/function and surviving `no_delete` rule and are covered by inference (all four installs verified in `pg_trigger`/`pg_rewrite`). Dev DB left pristine (all audit tables 0 rows, no sentinel residue, `registrations` untouched); only the pre-existing auth-email fixture user remains. Default suite stays hermetic at 156 (99 web + 57 domain; the 2 new integration tests self-skip among the 10 skipped); +2 opt-in DB tests (10 total opt-in); typecheck green.

## Oldest outcome

Unblocked Gate 7 (the owner provided the real sport-date dev database) and delivered the real-SQL integration coverage that the mocked unit tests could not prove. Ran the 18 additive migrations to bring the dev DB to current schema (it held only the empty `registrations` table), then added `apps/web/src/lib/auth-email.integration.test.ts` (8 tests) plus an opt-in runner (`scripts/test-integration.mjs`, `npm run test:integration`, gated on `RUN_DB_INTEGRATION=1`; the default suite stays hermetic and self-skips). The tests exercise the real `auth-email` functions against Postgres — verification single-use + sibling invalidation + expiry-without-consume, reset single-use + browser/mobile session revocation + password rotation, requester-IP-stored-as-hash, resend invalidation, and enumeration neutrality — using one bounded, reusable sentinel fixture user with per-test child-row cleanup (no per-test user churn). **Two real bugs surfaced that the mocks hid:** (1) `issueEmailVerificationTokenForUser` threw `invalid input syntax for type uuid: ""` on every empty-insert path (already-verified / non-active user) because `COALESCE((SELECT id FROM inserted), '')` coerced `''` to `uuid`; **fixed** by casting `id::text`. (2) **Systemic, not yet fixed:** no `users` row can be hard-deleted, because four append-only audit tables (`moderation_audit_log`, `moderation_case_access_log`, `moderation_evidence_references`, `role_audit_log`) pair an `ON DELETE SET NULL` FK to `users` with an `ON UPDATE … DO INSTEAD NOTHING` immutability rule — the rule swallows the FK's internal SET-NULL UPDATE and Postgres raises "referential integrity query … gave unexpected result." This blocks GDPR right-to-erasure of the underlying row (soft-delete has masked it). It is a safety/audit-control change that should be made as its own reviewed migration, not bundled here. The dev DB was left pristine (schema restored, all tables 0 rows, `registrations` untouched). Default suite unchanged at 156 (99 web + 57 domain); +8 opt-in DB tests; typecheck green.

## Earliest outcome

Closed the dangling reference from the Terms draft by writing `docs/legal/drafts/safety-community-guidelines.draft.md`, a member-facing Safety & Community Guidelines document headed "DRAFT — requires qualified EU counsel + a named safety owner review". The Terms draft (§4, §6) and its "How to read this draft" already pointed at `./safety-community-guidelines.draft.md`, which did not exist — the file now resolves that reference (no Terms edit needed). The document is grounded strictly in implemented behaviour, verified against the schema: blocking (`user_blocks`, immediate mutual-in-discovery revocation, never revealing who blocked whom), structured reporting (the real `safety_reports` category set), evidence as immutable references with **no file/message/screenshot uploads**, the real report→contain→triage→decide→notice→**one** appeal→audit lifecycle (decision codes and appeal statuses taken from `006`/`007` SQL; six-month appeal window; database-enforced reviewer separation per `moderator-access.md`), adults-only self-declared age, precise location revealed only after an accepted join, and the explicit absence of open messaging and any panic/emergency-dispatch feature. The honesty boundaries (no identity/age/background verification, no safety guarantee, not the organiser/chaperone, no emergency intervention or committed response time) repeat the Terms/Privacy boundaries verbatim in intent. Introduced a third honest marker, `[SAFETY-OWNER: …]`, for the staffing/response-time decisions that wait on owner Gate 5, alongside `[COUNSEL:]`/`[OWNER:]` for DSA mapping, jurisdiction-specific definitions, the emergency number, and the appeal-window confirmation — nothing invented. Indexed it in `docs/legal/drafts/README.md` (contents table + the marker convention). Cross-referenced and consistent with the Terms, Privacy Notice, consent copy, `moderation-operations.md`, and `moderator-access.md`; contradicts none. Docs-only: typecheck green; suite unchanged at 156 (99 web + 57 domain).

## Earliest (continued) outcome

Made the Product-energy principle "never reward swipes, skip decisions, rejection, report suppression, compulsive streaks, or time spent in the app" a regression-proof tested guard without altering any reward rule. Added `packages/domain/src/progress-never-rewards.test.ts` (10 tests) driving the real domain functions, plus two qualification-gate tests in `apps/web/src/lib/reflections.test.ts`. The key structural finding: the only pure-domain progression function, `calculateMovementProgress(attendedMoves: number)`, takes a single non-negative integer — none of the prohibited signals are even representable as inputs, so the guard is largely a tripwire over a structure that already cannot reward them. The one realistic wiring point, the qualification gate `qualifiesReflectionForProgress("attended")`, is pinned to exactly one outcome; skip/decline/rejection are exercised through the real `skipJoinRequest` (a join-request state, never a `MovementProgress`) and shown to yield zero moves. Each prohibited signal is covered individually as baseline+signal == baseline. Avoided one overclaim and documented it: `validateEventReflection` does not strip unknown keys, so the test asserts the qualifying *decision* is unchanged with vs without engagement-shaped extras, not that extras are stripped. Roadmap line checked `[x]`. Tests-only (no new guard function, to avoid an unwired second source of truth). Typecheck green; suite 144 -> 156 (web 97 -> 99, domain 47 -> 57).

## Completed and verified

- Established and validated the autonomous Product Studio operating system.
- Completed the account, event, discovery, request, and authorized room loops.
- Extended account export to hosted events, join requests, accepted seats, and precise locations already disclosed.
- Deletion requests now immediately cancel hosted events, close active requests, remove accepted seats, revoke room/location access, and revoke sessions.
- Added member blocking, structured safety reports, immediate shared-access revocation, and append-only moderation audit records.
- Added safety-report and block coverage to account export.
- Documented response priorities, evidence handling, appeals, staffing gates, and EU Digital Services Act preparation boundaries.
- Added a private member Safety Center with report states, reporter-safe decision notices, deadline enforcement, and one structured appeal per case.
- Extended account export to decision notices and member-submitted appeals.
- Added live-role-gated staff queues and case transitions with no self-promotion endpoint, no precise-location query, and revocation rechecks inside mutations.
- Added final decision notices with a named rule or legal basis, immutable audit events, and a six-month appeal window.
- Documented privileged role provisioning, revocation, and access review requirements.
- Added role-gated appeal review and reporter-visible outcomes, with database-enforced separation from the original decision-maker.
- Replaced sensitive moderation queue cards with metadata-only triage and audited case-specific access.
- Added immutable, purpose-limited evidence references with no file or copied-content ingestion and explicit retention-review dates.
- Added immutable access events for queue views, case views, and evidence-reference creation.
- Added private post-event attendance and willingness-to-join-again reflection for hosts with real participants and accepted members.
- Added a responsive private Movement Arc with five bounded stages, no leaderboard or streaks, and progress derived only from qualified self-confirmed attendance.
- Extended account export to private event reflections and documented the privacy, abuse, correction, and measurement boundaries.
- Added a clearly labelled Expo event-day interaction prototype with discovery, private reflection, and a shared-rule Movement Arc.
- Verified Expo SDK 56 configuration, mobile TypeScript, and a 571-module Android Hermes production bundle.
- Reviewed and documented the remaining moderate Expo build-chain advisories; no high or critical audit findings are present.
- Added separate native session families with 15-minute access tokens, fixed 30-day rotating refresh tokens, installation binding, spent-token reuse revocation, and account-deletion revocation.
- Added SecureStore-backed native credential handling, HTTPS/path restrictions, serialized refresh, optional live login, logout, and authenticated member restoration.
- Extended account export with native device-session metadata and documented remaining device-compromise and attestation risks.
- Verified Expo SDK 56 configuration and a 581-module Android Hermes bundle after native auth integration.
- Added native-authorized live approximate discovery, authorized hosted/accepted event selection, exact room access, private reflection, and live Movement Arc data.
- Added explicit mobile loading, empty, retry, stale access, lost room access, and reflection submission states.
- Verified a 582-module Android Hermes bundle after live product-data integration.
- Added native join requests and pending/accepted cancellation through shared web/mobile server mutations, including capacity, compatibility, mutual-block, seat-removal, destructive confirmation, and conflict refresh.
- Verified the Android Hermes bundle after native request mutation integration.
- Added shared web/native block and structured-report actions with immediate request, seat, room, and exact-location revocation.
- Added native host pending-request cards and shared accept/skip decisions with atomic numbered seats and third-skip decline.
- Added independently focusable mobile safety controls, emergency limitations, conflict refresh, and reporting for discovered hosts, pending requesters, room hosts, and other participants.
- Verified the production web build and Android Hermes bundle after native safety and host-decision integration.
- Added web/mobile device-session review with active, expired, current, and revoked states plus remote revocation without credential or installation-identifier exposure.
- Researched Bucharest local sport supply and recommended an Aviatorilor-Herastrau to Aurel Vlaicu-Floreasca validation corridor with run/run-walk and four-person indoor padel.
- Documented evidence limits, bouldering fallback, founding-host requirements, phased validation thresholds, kill criteria, and behavior-led member and host interview scripts.
- Prepared bilingual member and host recruitment copy, channel-specific organic drafts, privacy-aware form fields, campaign tags, decision signals, and a publication checklist without contacting or publishing to anyone.
- Added a local-only, noindex, non-collecting Bucharest research landing preview for owner review and verified that the mobile layout no longer overflows the viewport.
- Added tested app-wide browser response hardening with CSP, frame denial, referrer policy, nosniff, COOP/CORP, permissions policy, and production HSTS, then verified the headers from a running production build.
- Added tested app-layer rate limiting for browser login/signup, mobile login/refresh, join-request creation, and safety-report creation, including `429` retry metadata and a workspace Vitest config for alias-aware server-route tests.
- Added a repository-local Customer Experience Agent with privacy-safe ticket deduplication, separate parallel implementation ownership, and independent customer-perspective retesting.
- Added authenticated web and mobile product-feedback submission, private member-only history, shared bounded validation, explicit Safety Center routing, and account-export/deletion coverage.
- Verified the production web build and Android Hermes bundle after device-management integration.
- Implemented scheduled cleanup for expired browser sessions, spent mobile sessions, and outdated refresh-token history (`apps/web/src/lib/session-cleanup.mjs` with tests).
- Scaffolded email-verification and password-reset: dedicated request/confirm routes, a provider-gated delivery adapter (disabled by default, optional console simulation), bilingual-ready email drafts, single-use hashed tokens, and account-deletion token revocation, all wired into web pages and the profile surface.
- Added a direct unit suite for the verification/reset core: single-use consumption, expiry with no side effects, sibling-token invalidation, already-verified handling, requester-IP hashing instead of cleartext, and the reset boundary that deletes all browser sessions and revokes all mobile sessions.
- Added direct HTTP-route coverage for all four verification/reset routes (including `email-verification/request`, which previously had no test): success, already-verified, expired (410), invalid, validation-error, 503 unavailable, 401 unauthenticated, 403 cross-site, 429 rate-limit exhaustion, and password-reset enumeration neutrality across existing/missing accounts and database failure.
- Documented the verification/reset threat model in `docs/security/authentication.md`: per-threat control, cited test verification, and residual risk for token entropy/hashing, single-use + sibling invalidation, expiry windows, enumeration neutrality, the reset session-revocation boundary, requester-IP hashing, per-flow throttles, and the default-disabled provider-gated delivery seam; honest-claim boundary and owner/counsel/integration gates flagged.
- Consolidated the account-enumeration posture in `docs/security/authentication.md` and added a tested bounded timing floor to the unauthenticated password-reset request path, collapsing the common-case insert-vs-no-op latency difference while deferring full constant-time closure to the shared edge rate-limit gate.
- One hundred eighty-six hermetic tests pass (129 web + 57 domain); all workspaces type-check; 12 opt-in DB tests self-skip by default.
- Encoded the "never reward prohibited signals" principle as a regression-proof tested guard (`progress-never-rewards.test.ts` + reflection qualification-gate tests), covering swipes/views, skips, rejection, report suppression, streaks, and time-spent individually, without changing any reward rule.
- Authored `docs/operations/owner-escalation.md`, a consolidated owner decision packet covering every launch gate in the escalation format, leverage-ordered, so the owner can unblock multiple roadmap items in one sitting.
- Wrote `docs/legal/drafts/safety-community-guidelines.draft.md`, the member-facing Safety & Community Guidelines that the Terms draft referenced but did not exist, grounded in the implemented block/report/evidence/appeal behaviour and the schema, with `[COUNSEL:]`/`[OWNER:]`/`[SAFETY-OWNER:]` markers for everything not yet decided; indexed in the drafts README.
- Unblocked Gate 7 with the owner-provided dev database; added opt-in real-SQL integration tests for the verification/reset flows (`auth-email.integration.test.ts`, `npm run test:integration`), fixed an empty-insert `uuid` coercion bug they surfaced (`id::text`), and left the dev DB pristine.
- Fixed the systemic user-deletion / audit-immutability conflict (GDPR right-to-erasure): migration `019_audit_append_only_allows_user_nulling.sql` replaces the four `ON UPDATE … DO INSTEAD NOTHING` rules with `BEFORE UPDATE` triggers (one shared PL/pgSQL function) that block every application edit except the FK's non-null→NULL clearing of the user-reference column(s); the `no_delete` rules stay so audit rows remain app-undeletable. Proven against real SQL for `moderation_audit_log` and `role_audit_log` (`audit-erasure.integration.test.ts`): a referenced user is now hard-deletable, the audit row survives with the user-ref nulled and all else unchanged, and application UPDATE/DELETE of audit rows are still rejected.
- Shipped the credential-free Gate 1 deployment scaffolds: a `/api/health` liveness probe (no DB/provider), a Vercel-Cron-authorized `/api/internal/session-cleanup` endpoint reusing the existing cleanup core (bearer `CRON_SECRET`, **fails closed** when the secret is unset), `apps/web/vercel.json` (region `fra1` + daily cron), and `apps/web/.env.production.example` (placeholders only). Web suite 99 -> 106; hermetic 156 -> 163.
- Put the rate-limit storage behind a small `RateLimitStore` seam (in-memory `Map` default) with no behavior change — `rate-limit.test.ts` unchanged and green — and documented the Gate 6 Upstash REST adapter drop-in.
- **Implemented Gate 6 (shared/distributed rate limiting):** made the store + `consumeRateLimit`/`enforceRateLimit` async (Promise-returning `peek`/`bump` seam, exact two-phase fixed-window semantics preserved), updated all 12 `await` call sites, and added the env-gated Upstash Redis REST adapter (atomic pipeline INCR+PEXPIRE, GET+PTTL peek; activates only when both `UPSTASH_REDIS_REST_URL`/`_TOKEN` are set; **no new dependency**). On any Upstash error it gracefully degrades to the in-memory limiter (one warning) — never to unlimited. In-memory default is behavior-identical when the env vars are absent (proven by tests). New `rate-limit-upstash.test.ts` (11 mocked-`fetch` tests); hermetic suite 163 -> 174 (117 web + 57 domain); opt-in DB unchanged at 12; typecheck green. **Gate 6 is code-complete and activates when the owner provisions the Upstash store.**
- Closed the migration-019 residual "by inference" risk: added direct real-SQL erasure + immutability tests for the last two append-only audit tables (`moderation_case_access_log.actor_user_id`, `moderation_evidence_references.created_by_user_id`) in `audit-erasure.integration.test.ts`, building each minimal sentinel FK graph and proving (a) the referenced user hard-deletes while the row survives with only the user-ref nulled, (b) app UPDATE of a non-user-ref column and user-ref→non-null still raise, (c) app DELETE is still a no-op. **All four audit tables are now proven directly.** Opt-in DB tests 10 -> 12; hermetic suite unchanged at 163; dev DB left pristine.
- Added a **readiness** probe at `/api/health/ready` (one `SELECT 1` round-trip; `200 ready` / `503 not_ready` with a coarse, non-leaking `reason`), distinct from the `/api/health` liveness probe, so a monitor can tell "process up but DB unreachable" from a full outage; test asserts the body never leaks the connection string. Web suite 117 -> 122; hermetic 174 -> 179.
- Wrote `docs/operations/deployment-runbook.md` — the concrete first-deploy / migrations / forward-only rollback / secret-rotation / Neon-PITR backup / health-vs-readiness / Vercel-Cron / incident-response procedure for the ratified Neon+Vercel+Upstash+Sentry stack, with `[OWNER:]`/`[COUNSEL:]` markers for the undecided on-call owner, RTO/RPO, rotation cadence, and alert routing.
- **Wired Sentry error monitoring into the web app (`@sentry/nextjs@^10.62.0`):** env-gated on `NEXT_PUBLIC_SENTRY_DSN` (no DSN → `Sentry.init` is a no-op, behaviour identical to today), `withSentryConfig` verified to build on Next 16.2.9 (Turbopack) with `headers()`/`transpilePackages` preserved and source-map upload gated on `SENTRY_AUTH_TOKEN`. PII-scrubbed (`beforeSend` strips bodies/cookies/auth headers/query/email/IP; `sendDefaultPii: false`), **Session Replay OFF**. Server/edge/client configs + `instrumentation.ts` (`onRequestError`) + `global-error.tsx` + a shared scrubber with 7 tests. Web hermetic 122 → 129; full hermetic 179 → 186; typecheck + no-DSN/no-token production build green. Activates when the owner sets the Sentry DSN.

## Next three outcomes

1. **Await owner decisions on `docs/operations/owner-escalation.md`.** The remaining launch-leverage work is gated — counsel + controller identity + jurisdiction (Gates 1/2/3), named safety owner (Gate 5), email provider (Gate 4). The infra scaffolds (health, cron, `vercel.json`, env template) and the Gate 6 rate-limit Upstash adapter are now in place; what each remaining infra piece waits on: the Gate 6 Upstash adapter is code-complete and **activates automatically once the owner sets the Upstash env vars** (owner step 4) — only a real-Upstash opt-in integration test then remains; Sentry is now **wired and build-verified** and **activates automatically once the owner sets `NEXT_PUBLIC_SENTRY_DSN`** (owner step 5) — only a live test-error confirmation then remains; production migrations + the CI integration job wait on the Neon prod/test strings (Gate 1/7), and the uptime monitor waits on a deployed URL (owner step 6). Gate 7 is satisfied for the auth flows.
2. **Extend real-SQL integration coverage to the remaining security-sensitive flows** now that the deletion fix has landed and all four audit tables are proven directly. The remaining piece is exercising the erasure path END-TO-END through the application's full account-deletion code path rather than at the audit-table boundary (a direct `DELETE FROM users`).
3. (When unblocked) On Gate 6, the Upstash REST adapter is already in place and activates with the env vars — add the real-Upstash opt-in integration test once the store is provisioned; on Gate 5, finalise response-time commitments in the safety guidelines; on Gate 2, fold counsel's answers back into the four drafts.

## Owner blockers

- See `docs/operations/owner-escalation.md` for the consolidated decision packet bundling every owner-only launch gate (infrastructure, EU counsel, brand/country, email provider, safety owner, edge rate limiting, isolated test DB), each in the escalation format and ordered by leverage.
- Choose or approve research toward the first launch country and city before real outreach.
- Provide or authorize infrastructure credentials only when production services are selected.
- Approve final brand and personally create or authorize external social accounts before publication.
- Qualified European counsel must approve final retention periods, lawful bases, privacy notices, and deletion exceptions.
- Name trained safety owners and an escalation rota before critical-response targets or member messaging can go live.

## Active assumptions and risks

- "Sport Date" is a working name; Bucharest is a research hypothesis.
- Third skip currently means automatic decline and awaits confirmation.
- Deletion finalization and vendor/backup propagation remain operational launch gates. The previous blocker — hard-deleting a `users` row was impossible (audit-immutability rule vs `ON DELETE SET NULL` FK across four audit tables) — is now fixed by migration `019` (BEFORE UPDATE triggers permitting only the FK's non-null→NULL clearing), proven directly for ALL FOUR audit tables (`moderation_audit_log`, `role_audit_log`, `moderation_case_access_log`, `moderation_evidence_references`) against real SQL; the previous "by inference" gap for the last two is now closed. The remaining erasure work is the application's full account-deletion code path end-to-end (not just the audit-table boundary).
- Shared/distributed rate limiting (Gate 6) is now **code-complete**: the async `RateLimitStore` seam plus an env-gated Upstash Redis REST adapter (atomic pipeline INCR+PEXPIRE, in-memory graceful-degradation fallback on error, no new dependency) ship in `apps/web/src/lib/rate-limit.ts`. It **activates automatically once the owner provisions Upstash** and sets `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`; until then the per-process in-memory limiter is the default and is behavior-identical. Residual: the Upstash path is unit-tested against a mocked `fetch`, not a live instance — a real-Upstash opt-in integration test (cross-process counter, real REST encodings/TTL) would close that once the store exists. A separate edge/gateway (CDN-level) control remains a distinct, optional future layer.
- Product feedback has no cross-member staff queue until access ownership and retention are approved; coding agents use the separate repository-local feedback queue.
- End-to-end feedback rejection and accessibility behavior still needs an isolated signed-in fixture plus physical-device or browser assistive-technology testing.
- Database-level integration tests now exist for the verification/reset auth flows (opt-in, run against the owner-provided dev DB via `npm run test:integration`); a dedicated disposable/CI Postgres is still preferable to running them against the shared dev DB, and coverage of other security-sensitive flows is not yet written.
- No social, safety, verification, or traction claims may exceed implemented evidence.
