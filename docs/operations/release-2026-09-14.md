# Approved experience release — 14 September 2026

Owner decision `SD-20260914-release-reviewed-experience` was read from the authenticated live HQ API as approved, with no additional comment. This authorizes only the reviewed experience/framework release.

## Exact candidate and deployment

Isolated branch `release/reviewed-20260914`, commit `d7064b78eaf3624d208ab7494184820c27fca418`: base `d58c0e5` plus only `apps/web/package.json` and `package-lock.json` copied byte-for-byte from approved patch `98ac494`. Git comparisons prove all other files equal the approved base. No later autonomous joining/discovery application changes are included. No Git remote push was performed.

Existing Vercel project `sport-date` (`prj_jg5QHVbyVwxGDzuTxlpPFIjnCMfY`), root `apps/web`. Dry-run verified every uploaded source path is tracked; no local credentials or runtime files uploaded. Candidate deployment `dpl_EjVfwK7QgEFbmeuAyL3RXxEVkzTc`, URL https://sport-date-84t3kcyvw-emrullah-yildizs-projects.vercel.app . Vercel metadata identifies the exact candidate commit.

The CLI was given a temporary external Vercel config with `buildCommand: next build`; API project settings initially reported that override. However, remote build logs show Vercel reread the uploaded `apps/web/vercel.json` and executed its original migration hook. The hook explicitly reported **0 new migrations applied** at 2026-09-13T23:05:20.654Z: production was already current. Its existing `CREATE TABLE IF NOT EXISTS schema_migrations` and ledger reads ran; no pending schema migration executed. Do not treat the external config override as an effective migration bypass in future monorepo releases.

Repository configuration and persistent project settings remain unchanged. Runtime region remains `fra1`, function duration and both cron schedules are unchanged (build execution itself occurred in Vercel's `iad1` builder). The candidate has no database migration file differences from previously live `7fe07ad`; changed application files do not require new schema. No cron endpoint was invoked manually.

## Verification

Fresh `npm ci` succeeded. All workspace typechecks passed. Candidate tests passed: 1,190 web and 229 domain; 14 database opt-in tests skipped. Lint passed with zero errors and 11 existing warnings. Initial individual-file upload failed before deployment creation; one archive upload retry succeeded.

Vercel reported **Ready** and production aliases `keepitup.social` and `www.keepitup.social` on this deployment. Remote Next.js 16.3.5 production build completed in 46 seconds. Live checks at approximately 2026-09-13T23:06Z passed: `/api/health` returned 200 `ok`, `/api/health/ready` returned 200 `ready`, `/` redirected to `/landing` successfully, and `/landing`, `/hq.html`, and the host-pilot packet returned 200. Both static HQ and host-pilot HTML match candidate bytes by SHA-256. Persistent project build command remains null and root directory remains `apps/web`.

Cron authorization, Redis connectivity, email delivery and signed-in journeys are not established by these checks. Public readiness verifies a database round-trip, not a customer/activation/revenue baseline. Root coordinator owns subsequent current HQ report publication and agent-state reconciliation.
