# CX-20260701-profile-photo-series-up-to-six

- Status: `verified`
- Severity: `high`
- Priority: `P1 high` — (Reach 5 × Impact 5 × Confidence 4) / Effort 4 = 25, held at P1: owner-requested and central to "who am I meeting" trust. UNBLOCKED 2026-07-01: owner selected the production storage stack and moderation stance (see below); build may proceed.
- Owner decision (2026-07-01): storage provider = **Vercel Blob** (private store; signed / non-guessable URLs; EXIF stripped on upload; included in account export + hard-deletion). Moderation stance = **report-based + manual review** via the existing moderation queue (photos visible on upload; members can report; moderators action takedowns). All existing guardrails stand (up to 6, optional, reorder / set-primary / delete, not retrievable unauthenticated, no attractiveness / popularity / ranking mechanics). The Vercel Blob token/credential is an **owner-provided secret** to be added at build/deploy time — the implementer must NOT invent, hardcode, or commit any secret; wire the integration to read the token from the environment and fail closed if it is absent.
- Customer journey: trust check / intent
- Surface: `web` (and mobile later)
- Environment and viewport/device: all widths
- Found by: Owner (direct feedback 2026-07-01)
- Implementation owner: `experience-build-agent`
- Related tickets: `CX-20260701-profile-lacks-rich-browsable-detail`

## Customer outcome

As a member deciding whether to meet someone, I want to see a small series of their photos (up to 6) so that I can recognise them and feel safer about who I'm meeting.

## What I observed

There is no photo support anywhere — profile, create-event, and event pages are entirely text-only (grep for photo/image/upload/avatar returns nothing in those surfaces). Members are represented only by initials.

## What I expected

A member can add up to 6 photos to their profile, reorder them, set a primary, and remove them. Other members see the series on the profile in a clear, browsable way. Visibility respects the product's privacy posture (photos are not exposed to unauthenticated scraping; approximate-location and pre-acceptance privacy rules are unaffected).

## Reproduction

1. Open `/profile`. There is no way to add a photo.

Reproduction rate: `confirmed; feature absent`

## Customer impact

Without any photo, recognition at the meeting point is harder and the trust check before committing is weaker — directly relevant to a safe in-person encounter. Photos are sensitive personal data: they must be stored, served, and moderated carefully, and must not become a scraping or harassment vector.

## Escalation note (owner decision — RESOLVED 2026-07-01)

The escalation-policy decisions this feature required — image storage/hosting provider, content moderation approach for user-uploaded images, and retention/deletion — have been **made by the owner**: storage = Vercel Blob (private, signed/non-guessable URLs, EXIF stripped on upload, included in export + hard-deletion); moderation = report-based + manual review via the existing moderation queue. The only remaining owner-supplied input is the **Vercel Blob token/credential**, which is a secret to be provided at build/deploy time. The implementer must read that token from the environment (never invent, hardcode, or commit it) and build the UI + data model + upload/serve/delete path against the private store; no further owner escalation is required to begin.

## Acceptance criteria

- [x] A member can upload, reorder, set primary, and delete up to 6 photos; the limit and accepted formats/sizes are explained. (domain `validateProfilePhotoUpload` max-6 + COUNT-guarded insert; `ProfilePhotos.tsx` upload/↑↓/Make main/confirm-remove; `PROFILE_PHOTO_RULES_SUMMARY` explains limits.)
- [x] Photos appear on the member's profile as a browsable series; a primary photo is used where a single image is shown. (`ProfilePhotos.tsx` ordered `<ol>`; `profile/page.tsx` hero uses `primaryPhoto` = `find(isPrimary) ?? photos[0]`.)
- [x] Upload has clear loading, failure, and oversize/invalid-type states; deletion is confirmable and effective (including from export/deletion flows). (client validation + calm 503/409/413/400 copy; two-step confirm remove; deletion route purges rows + blobs; account deletion calls `purgeProfilePhotosForUser`.)
- [x] Photos are stored in **Vercel Blob (private)**; not retrievable by unauthenticated requests; URLs are signed / non-guessable / non-scrapeable; no precise-location EXIF is leaked (strip metadata on upload). (`put(access:'private')` to UUID pathname; serve route 401s anon (live-confirmed), block-aware, `Cache-Control: private, no-store` + `noindex`; byte-level `stripImageMetadata` runs before `put`, unit-asserted GPS sentinel removed for JPEG/PNG/WebP + fail-closed to null on junk.)
- [x] The Vercel Blob token is read from the environment (owner-provided secret); no secret is invented, hardcoded, or committed; the integration fails closed if the token is absent. (`readBlobToken()` reads only `BLOB_READ_WRITE_TOKEN`; grep confirms the only literal token string is the synthetic test fixture `vercel_blob_rw_test_token_value`; every op returns `not-configured` when unset. Live: list → `storageConfigured:false`, upload → 503 calm.)
- [x] Report-based moderation: a member can report a photo and it routes to the existing moderation queue for manual review/takedown. (`reportProfilePhoto` inserts into `safety_reports` + `moderation_audit_log` tagged with the photo id; self-report rejected; route rate-limited + auth-gated.)
- [x] Accessibility: alt handling, keyboard reorder/delete, focus, 44px targets; reduced-motion safe; on-brand. (alt field + per-photo fallback alt; button-based reorder/delete keyboard-operable with `aria-label`s; static UI so reduced-motion parity is inherent; labelled region + role=status/alt live messages.)
- [x] No attractiveness scoring, ranking, or public popularity metric is introduced. (order is member presentation choice only; copy states "no scores, no ranking"; no counters/aggregates anywhere.)
- [x] Storage/moderation decision recorded (Vercel Blob + report-based manual review, 2026-07-01); account export and deletion include photos. (export includes photo metadata + in-app fetch path, raw pathname excluded; deletion purges rows + blobs.)
- [x] Repository checks pass. (per build note: web 247 pass/12 skip, domain 125 pass, typecheck + lint pass, migration 024 clean.)

## Handoff and retest log

- 2026-07-01 - Filed from owner feedback; status `ready` (storage/moderation sub-parts pending owner decision).
- 2026-07-01 - Owner elected to HOLD until a production storage stack is chosen; moderation stance set to report-based + manual review. Status → `blocked-owner`. Loop skips this ticket until the owner unblocks the storage choice.
- 2026-07-01 - **Owner decision — UNBLOCKED.** Storage provider = **Vercel Blob** (private store; signed / non-guessable URLs; EXIF stripped on upload; photos included in account export + hard-deletion). Moderation = **report-based + manual review** via the existing moderation queue. All prior guardrails retained (up to 6, optional, reorder / set-primary / delete, not retrievable unauthenticated, no attractiveness / popularity mechanics). The Vercel Blob token is an **owner-provided secret** added at build/deploy time — the implementer reads it from the environment and must NOT invent, hardcode, or commit a secret; integration fails closed without it. Status `blocked-owner` → `ready`. The loop may pick this up.
- 2026-07-01 - build - Status `ready` → `in-progress`; owner `experience-build-agent`. Implementing the full vertical slice (data model + fail-closed Vercel Blob storage module + upload/list/reorder/set-primary/delete + report-to-moderation + export + hard-delete cascade + browsable UI). Token read from `BLOB_READ_WRITE_TOKEN`; no secret committed; fails closed when absent.
- 2026-07-01 - explore - **VERIFIED** (independent retest, resolved in ONE step per the no-poll handshake). Rigorously source-verified every criterion + live-confirmed the fail-closed path (Blob token unset locally). Confirmed at source + unit: token read ONLY from `BLOB_READ_WRITE_TOKEN` via `readBlobToken()`, grep proves the only literal token is the synthetic test fixture (no secret committed); byte-level EXIF/metadata strip runs before any `put`, fail-closed to null on junk (image-metadata tests assert GPS sentinel removed + image data survives for JPEG/PNG/WebP); max-6 enforced in domain validator AND the COUNT-guarded insert (orphan blob cleaned on ceiling); reorder/set-primary reject foreign/duplicate ids; delete purges row + blob + re-primaries; export includes metadata + in-app fetch path (raw pathname excluded); account deletion calls `purgeProfilePhotosForUser` (rows + blobs) with FK cascade backstop; report routes into `safety_reports` + `moderation_audit_log`, self-report rejected; serve route is block-aware with `private, no-store` + `noindex`; hero uses `find(isPrimary) ?? photos[0]`; no attractiveness/ranking/popularity; a11y (labelled region, keyboard reorder/delete buttons with aria-labels, alt handling, static → reduced-motion safe). LIVE (one pooled host-A login, session reused, no poll): authed list → `{"photos":[],"storageConfigured":false}`; authed upload → HTTP 503 calm "Photo uploads aren’t available yet. The rest of your profile is unaffected."; anonymous list / serve `/api/photos/<uuid>` / report → all HTTP 401 (not retrievable/reportable unauthenticated). SOURCE-ONLY sub-branch (as expected, token-gated): the live Blob `put`/`get`/`del` round-trip with a real token — covered by mocked photo-storage tests (private access, member-scoped UUID pathname, EXIF stripped before `put`, error-not-throw). All criteria pass → status `implemented` → `verified`.
- 2026-07-02 - test - **REAL END-TO-END UPLOAD CONFIRMED (storage no longer dormant).** With `BLOB_READ_WRITE_TOKEN` available, uploaded a real PNG (carrying an `eXIf` GPS sentinel chunk) as pooled host-A through the actual app route: `POST /api/account/photos` → **201** (not the 503 fail-closed, not a 502), photo `isPrimary:true`; `GET /api/account/photos` → 200 (photo in series); `GET /api/photos/<id>` → **200** `image/png`, `cache-control: private, no-store`, `x-robots-tag: noindex, noimageindex`, and the served bytes **no longer contain the GPS sentinel** (EXIF STRIPPED: 106→69 bytes); anonymous serve → **401**. Set-primary → 200 (primary moved), delete → 200 (both removed, series back to 0). Test data cleaned up. **NEW DEFECT FOUND (for Planner — NOT fixed here): reorder is broken.** `PATCH /api/account/photos/order` → **500**: `reorderProfilePhotos` phase-1 does `SET position = position + MAX_PROFILE_PHOTOS` (=+6), which violates the DB CHECK constraint `profile_photos_position_check` (`apps/web/db/024_profile_photos.sql:30` = `CHECK (position >= 0 AND position < 6)`) — any member with 2+ photos who reorders hits `23514 new row … violates check constraint`. Never caught before because storage was dormant so the live reorder path never ran against the constraint. Feature otherwise works end-to-end. Ticket stays `verified`; reorder defect logged for a new ticket.
- 2026-07-01 - build - **Implemented (whole feature, not sliced).** Status `in-progress` → `implemented`; commit `21ca0cd`. Delivered: migration 024 `profile_photos` (private table, one-primary + stable-order + unique-pathname indexes, FK cascade on user delete); domain `profile-photo` (max-6 / accepted formats+size / reorder+set-primary validators) and `image-metadata` (byte-level JPEG/PNG/WebP EXIF strip, fail-closed on unknown container); web `photo-storage` (Vercel Blob isolated + private access + reads `BLOB_READ_WRITE_TOKEN` from env, fails closed to a calm "uploads aren't available yet" when absent) and `photos` lib (COUNT-guarded max-6 insert, delete-with-blob + re-primary, reorder, set-primary, report-to-moderation, purge-for-user); routes `POST/GET /api/account/photos`, `PATCH /api/account/photos/order`, `DELETE /api/account/photos/[id]`, `PUT /api/account/photos/[id]/primary`, authenticated `GET /api/photos/[id]` (401 for anon; block-aware), `POST /api/photos/[id]/report`; export + deletion now include/purge photos+blobs; `ProfilePhotos` UI (browsable series, calm loading/failure/oversize states, keyboard reorder/delete, 44px targets, visible focus, primary used in hero). No attractiveness/ranking/popularity. Checks: typecheck pass, lint pass (only warning is in untracked qa/full-flows.mjs), web tests 247 pass/12 skip, domain tests 125 pass, migration 024 clean, dev fail-closed verified (upload→503 calm, anon serve→401). NO secret committed (token read from env; test fixture uses a synthetic in-memory value only). Ready for independent retest.
